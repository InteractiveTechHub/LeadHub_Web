networks:
  interactive_net:
    external: true

services:
  leadshub_web:
    container_name: "leadshub_web"
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - 80
    networks:
      interactive_net:
        ipv4_address: 172.19.0.10
