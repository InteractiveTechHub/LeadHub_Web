worker_processes 1;

events {
    worker_connections 1024;
}

http {
  #resolver 127.0.0.11 valid=30s;  # DNS do Docker

  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;

  server {
    listen       80;
    server_name  www.tecnoprohub.com tecnoprohub.com;

    root /usr/share/nginx/html;
    index index.html;
    error_page 404 /index.html;

    location / {
      try_files $uri $uri/ /index.html;
      expires -1;
      add_header Cache-Control "no-store, no-cache, must-revalidate, max-age=0";
      add_header Pragma "no-cache";
    }

    # # LeadsHub api configuration
    # location /api/ {
    #   set $backend "http://leadshub_api:8080";
    #   proxy_pass $backend;
    #   proxy_set_header Host $host;
    #   proxy_set_header X-Real-IP $remote_addr;
    #   proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #   proxy_set_header X-Forwarded-Proto $scheme;

    #   proxy_connect_timeout 5s;
    #   proxy_read_timeout 10s;
    #   proxy_send_timeout 10s;

    #   proxy_intercept_errors on;
    #   error_page 502 503 504 = @fallback;
    # }

    # #Websocket configuration
    # location /leadhub {
    #   set $backend_ws "http://leadshub_api:8080";
    #   proxy_pass $backend_ws;
    #   proxy_http_version 1.1;
    #   proxy_set_header Upgrade $http_upgrade;
    #   proxy_set_header Connection 'upgrade';
    #   proxy_set_header Host $host;
    #   proxy_cache_bypass $http_upgrade;

    #   proxy_connect_timeout 5s;
    #   proxy_read_timeout 10s;
    #   proxy_send_timeout 10s;

    #   proxy_intercept_errors on;
    #   error_page 502 503 504 = @fallback;
    # }

    # location = @fallback {
    #   return 200 "API fora do ar. Tente novamente mais tarde.";
    #   add_header Content-Type text/plain;
    # }
  }
}
